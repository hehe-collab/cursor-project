---
description: 自动将所有对话追加到记录文件（所有 Agent 遵循）
alwaysApply: true
---

# 对话自动记录规则

**本规则对本项目内所有 Agent 生效**。在 `.cursor/rules/` 下的规则会应用于当前工作区的所有对话会话。

## 必须执行

每次回复用户后，在回复结束前**自动**将本次对话要点追加到以下 **3 个文件**（同步备份，防止损坏）：

1. `/Volumes/存钱罐/cursor项目/对话记录.md`
2. `/Volumes/存钱罐/cursor项目/对话记录-备份1.md`
3. `/Volumes/存钱罐/cursor项目/对话记录-备份2.md`

**追加时需同时更新上述 3 个文件**，保持内容完全一致。

## 记录范围

**记录所有内容**，不限主题。包括但不限于：
- 编程、Git、GitHub、技术问题
- 项目开发、代码修改
- 工具使用、配置问题
- 其他任何有实质内容的对话

## 追加格式

在文件末尾追加：

```markdown
---

## [YYYY-MM-DD] 对话记录

**用户问题**：（简要概括用户的问题或需求）

**要点**：
- 要点1
- 要点2
- （重要命令、配置、路径等保留原文）
```

## 不追加的情况

- 用户仅打招呼（如"你好"）且无后续实质内容
- 用户明确说"不用记录"或"不要记录"

## 其他 Agent 如何遵循

本规则位于项目根目录 `.cursor/rules/` 下，Cursor 会自动加载。只要在该项目内对话，**所有 Agent（Chat、Composer 等）都会应用此规则**，无需额外配置。

若要在其他项目中使用，需将该规则文件复制到该项目的 `.cursor/rules/` 目录。
