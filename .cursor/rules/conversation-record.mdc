---
description: 自动将所有对话追加到记录文件（所有 Agent 遵循）
alwaysApply: true
---

# 对话自动记录规则

**本规则对本项目内所有 Agent 生效**。在 `.cursor/rules/` 下的规则会应用于当前工作区的所有对话会话。

## 【强制】必须执行

**这是回复流程的必选步骤**：每次回复用户后，在回复结束前**必须**使用 StrReplace 或 Write 工具将本次对话要点追加到以下 **2 个文件**（主文件 + 一份备份，防止损坏）。未执行则视为未完成回复。

1. `/Volumes/存钱罐/cursor项目/对话记录.md`
2. `/Volumes/存钱罐/cursor项目/对话记录-备份1.md`

**追加时需同时更新上述 2 个文件**，保持内容完全一致。

## 记录范围

**记录所有内容**，不限主题。包括但不限于：
- 编程、Git、GitHub、技术问题
- 项目开发、代码修改
- 工具使用、配置问题
- 其他任何有实质内容的对话

## 追加格式

在文件末尾追加：

```markdown
---

## [YYYY-MM-DD] 对话记录

**用户问题**：（简要概括用户的问题或需求）

**要点**：
- 要点1
- 要点2
- （重要命令、配置、路径等保留原文）
```

## 不追加的情况

- 用户仅打招呼（如"你好"）且无后续实质内容
- 用户明确说"不用记录"或"不要记录"

## 【强制】Secret 脱敏规则

追加内容前**必须**对以下类型的敏感信息进行脱敏处理（替换为 `***REDACTED***` 或仅保留前 6 位 + `...`）：

| 敏感类型 | 脱敏方式 |
|---------|---------|
| 阿里云 AccessKey ID（`LTAI...`）| 保留前 6 位：`LTAI5t...` |
| 阿里云 AccessKey Secret | 完全替换：`***REDACTED***` |
| 数据库密码、应用密码 | 完全替换：`***REDACTED***` |
| JWT_SECRET、ENCRYPT_SECRET_KEY 等 | 完全替换：`***REDACTED***` |
| API Token / Bearer Token | 完全替换：`***REDACTED***` |
| 私钥（`-----BEGIN PRIVATE KEY-----`）| 完全省略，记为 `[私钥已省略]` |
| Cookie / Session Token | 完全替换：`***REDACTED***` |
| 手机号完整号码 | 保留区号 + 末 4 位：`+86 ****1234` |
| 邮箱完整地址 | 保留账号前 3 位 + 域名：`abc***@gmail.com` |

**绝对禁止**：把命令输出（如 `cat /etc/*.env`、`echo $SECRET`、`grep PASSWORD ...`）的原始内容直接写入记录文件 —— 必须先脱敏。

### 脱敏示例

❌ 错误：
```
DB_PASSWORD=<真实数据库密码原文>
```

✅ 正确：
```
DB_PASSWORD=***REDACTED***
```

❌ 错误：
```
新 AK: <完整 AccessKey ID> / <完整 AccessKey Secret>
```

✅ 正确：
```
新 AK ID: LTAI5t...（已存到 1Password），Secret: ***REDACTED***
```

## 【强制】.gitignore 与本规则的协同

- 所有对话记录文件（包括备份）**必须**被 `.gitignore` 覆盖
- 即便如此，记录内仍要遵守上方脱敏规则（防止本地文件被其他途径泄露）
- 已有 `pre-commit` hook (`scripts/check-secrets.sh`) 兜底，但**脱敏是第一道防线**

## 其他 Agent 如何遵循

本规则位于项目根目录 `.cursor/rules/` 下，Cursor 会自动加载。只要在该项目内对话，**所有 Agent（Chat、Composer 等）都会应用此规则**，无需额外配置。

若要在其他项目中使用，需将该规则文件复制到该项目的 `.cursor/rules/` 目录。

## 若发现记录缺失

- **原因**：规则依赖 AI 在每次回复时主动执行追加，若未执行则不会写入。
- **处理**：用户可提醒「请把本次对话追加到记录」，或定期检查并手动补录。
