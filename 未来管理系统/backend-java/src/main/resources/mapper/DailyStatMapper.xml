<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.drama.mapper.DailyStatMapper">
  <select id="selectById" resultType="com.drama.entity.DailyStat">
    SELECT * FROM daily_stats WHERE id = #{id}
  </select>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO daily_stats (stat_date, metric, metric_value) VALUES (#{statDate}, #{metric}, #{metricValue})
  </insert>
  <update id="update">
    UPDATE daily_stats SET metric_value = #{metricValue} WHERE id = #{id}
  </update>
</mapper>
