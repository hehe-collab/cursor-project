<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.drama.mapper.UserWatchHistoryMapper">
  <select id="selectById" resultType="com.drama.entity.UserWatchHistory">
    SELECT * FROM user_watch_history WHERE id = #{id}
  </select>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO user_watch_history (user_pk, drama_id, episode_num, progress_sec)
    VALUES (#{userPk}, #{dramaId}, #{episodeNum}, #{progressSec})
  </insert>
  <update id="update">
    UPDATE user_watch_history SET episode_num=#{episodeNum}, progress_sec=#{progressSec} WHERE id=#{id}
  </update>
  <delete id="deleteById">
    DELETE FROM user_watch_history WHERE id = #{id}
  </delete>
</mapper>
