# ECS 发版记录 · 2026-04-20 · VOD 端到端 + 短剧批量上传

把上一轮（[VOD-联调验证报告-2026-04-20.md](协作文档/VOD-联调验证报告-2026-04-20.md)）所有代码改动推送到生产 ECS。

## 一、发版时序

| 时间 | 步骤 | 结果 |
|---|---|---|
| 11:00 | 本机 git commit `9d5a07c` | ✅ pre-commit secret hook 通过 |
| 11:05 | 本机 `mvn -DskipTests package` 产出 jar 83MB | ✅ 2.4s 增量构建 |
| 11:06 | 本机 `npm run build` 产出 dist 4.5MB | ✅ |
| 11:07 | scp jar + rsync dist + scp deploy-frontend.sh 到 ECS | ✅ |
| 11:08 | `bash /data/scripts/deploy-backend.sh /tmp/drama-admin-new.jar` | ✅ 12s 健康通过 |
| 11:09 | `bash /data/scripts/deploy-frontend.sh /tmp/drama-frontend-new` | ⚠️ dist 已替换，但脚本因 pipefail+grep -q 误判失败 |
| 11:10 | 修脚本（用 `var=$(cmd 2>&1)` 捕获再 grep）+ 重传 + `--check` 验证 | ✅ |
| 11:11 | 生产冒烟全绿 | ✅ |
| 11:14 | commit `716ef80` 修复后的 deploy-frontend.sh | ✅ |

## 二、生产现状

```
后端：
  /data/app/drama-admin-0.0.1-SNAPSHOT.jar           (83MB, 2026-04-20 11:08:34)
  /data/backup/jars/drama-admin_20260420_110834.jar.bak  (旧版回滚备份)
  systemd: drama-admin active (running)

前端：
  /data/web/dist/                                     (4.6MB, 2026-04-20 11:06:08)
  /data/backup/web/dist_20260420_110908/              (旧版回滚备份)
  https://admin.hookedshorts.com/  → HTTP 200

新增运维脚本：
  /data/scripts/deploy-frontend.sh

actuator/health：
  overall: UP
  components: database / db / diskSpace / ping / vod (UP, 已配置)
```

## 三、回滚预案

```bash
# 后端回滚到上一份备份
sudo /data/scripts/deploy-backend.sh --rollback

# 前端回滚到上一份备份
sudo /data/scripts/deploy-frontend.sh --rollback

# 列出可回滚的备份
sudo /data/scripts/deploy-backend.sh --list
sudo /data/scripts/deploy-frontend.sh --list
```

## 四、本轮发现 + 修的脚本 bug

`deploy-frontend.sh` 第一版踩了 `set -uo pipefail + grep -q` 经典 SIGPIPE 陷阱：

```bash
sudo nginx -t 2>&1 | tee -a "$LOG_FILE" | grep -qE "syntax is ok|test is successful"
```

`grep -q` 一旦匹配立即 exit 0 关闭 stdin → 上游 `tee` 收 SIGPIPE → pipefail 让管道返回非零 → 误判「nginx -t 失败」。

修复方式：把命令输出捕获到变量，再单独 grep 检测，避开管道：

```bash
nginx_t_out=$(sudo nginx -t 2>&1) || true
echo "$nginx_t_out" | tee -a "$LOG_FILE" >/dev/null
if echo "$nginx_t_out" | grep -E "syntax is ok|test is successful" >/dev/null; then
    ok "nginx -t 通过"
fi
```

同样应用到 `nginx -s reload` 检测段。

## 五、影响范围 + 解锁

- **解锁**：之前「未发版前请勿在 admin.hookedshorts.com 上传新视频」的限制现已取消，生产 admin 可以正常上传
- **不影响**：H5 端 / 用户端无任何改动
- **不影响**：数据库 schema 无变更（drama_episodes 表此前由 `DramaEpisodeVodColumnsInitializer` 自动补列）

## 六、用户验收清单

浏览器打开 `https://admin.hookedshorts.com`，**Cmd+Shift+R 强刷**（务必清旧 JS 缓存）：

1. 登录
2. 短剧管理 → 修改任意一部剧
3. 弹窗里应看到「视频文件 [选取文件]」批量上传 UI
4. 选 1-2 个 mp4 试上传 → 进度条 + 中文「上传中 / 已上传 / 已就绪」tag
5. 点确定保存

## 七、未做的事

- 没有 git push 到 GitHub（用户授权 commit 但暂缓 push）
- 没有清理 ECS `/tmp/drama-admin-new.jar` `/tmp/drama-frontend-new/`（系统重启时自动清）
- 没有改默认管理员密码（生产应已改过；如未改请单独处理）
